describe('types in typescript ', () => {

    describe('variables and constants in typescript ', () => {
        it('using let to decalre a variable', () => {
            let x;
            x = 3.14;
            expect(x).toBe(3.14);
            x = 'tacos'
            expect(x).toBe('tacos');
        })

    });

});