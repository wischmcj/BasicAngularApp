describe('calculator', () => {

    describe('addition', () => {

        it('can add two numbers', () => {
            const a = 10;
            const b = 20;
            const answer = a + b;
            expect(answer).toBe(30);
        })

        it('using let with type in type script', () => {
            let x: Number;
            x = 3.14;
            x = 12;

        })

    })
})